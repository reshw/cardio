# 수정사항 260414

## 1. ClubMemberDetailModal — 운동 카드 좋아요/댓글 기능 추가

**변경 내용**
- 각 운동 카드 하단에 하트(좋아요) + 말풍선(댓글) 버튼 추가
- 말풍선 클릭 시 해당 카드 아래에 CommentSection 토글 (댓글 목록/작성/삭제/답글)
- 카드 본문 클릭(상세)과 버튼 이벤트 분리
- 좋아요 낙관적 업데이트 (실패 시 롤백)
- 월 전환 시 좋아요/댓글 수 배치 쿼리로 한 번에 로드 (N+1 방지)

**변경 파일**
- `src/components/ClubMemberDetailModal.tsx`
- `src/services/feedService.ts` — `getLikesInfoForWorkouts`, `getCommentCountsForWorkouts` 추가
- `src/App.css` — `.member-card-actions`, `.member-card-comments` 추가

---

## 2. ClubMemberDetailModal — 운동일수 표시

**변경 내용**
- "총 운동 N회" → "운동일수 N일"로 변경
- enabled_categories 기준 인정 운동만 포함
- 같은 날 복수 기록 → 1일로 카운트

**변경 파일**
- `src/components/ClubMemberDetailModal.tsx`

---

## 3. WorkoutFeed — 날짜 UX 개선

**변경 내용**
- 날짜 텍스트 클릭 시 네이티브 캘린더 피커 열림 (원하는 날짜 바로 이동)
- 오늘이 아닌 날짜를 보고 있을 때 "오늘" 버튼 우측 고정 노출 → 클릭 시 오늘로 복귀
- 오늘 날짜에서 우측 화살표 비활성화

**변경 파일**
- `src/components/WorkoutFeed.tsx` — `onDateSelect` prop 추가, 캘린더 input, 오늘 버튼
- `src/pages/Club.tsx` — `handleDateSelect` 추가, prop 전달
- `src/App.css` — `.date-nav-today-btn` (position: absolute; right) 추가

---

## 4. 마일리지 보드 검색 UX 개선

**변경 내용**
- 검색 모달 열릴 때 BottomNav 숨김 (body class), 닫히면 복귀
- 검색 모달 상단 고정 (`modal-overlay--top`) — 키보드 올라와도 모달 안 가려짐
- Enter 키 입력 시 `blur()` → 모바일 키보드 내려감
- 검색어 없을 때 결과 없음 (빈 화면)
- 검색어 입력 시 매칭 본인만 노출
- 선택 후 리더보드: 해당 멤버 기준 위 3명 + 본인 + 아래 5명 표시

**변경 파일**
- `src/pages/Club.tsx`
- `src/App.css` — `.modal-overlay--top`, `body.search-modal-open .bottom-nav` 추가

---

## 5. 더보기 — 수정 요청 / 버그 제보 기능

**변경 내용**
- 더보기 페이지 "앱 정보" 섹션에 "수정 요청 / 버그 제보" 메뉴 추가
- 제목(input) + 내용(Jodit 리치 에디터) 입력 폼
- 전송 시 Supabase Edge Function(`send-feedback`) → Resend → reshw@naver.com 발송
- 메일 본문에 발신자 이름 / 휴대폰 / 이메일 자동 포함
- 발신 주소: `noreply@scnd.kr`

**변경 파일**
- `src/pages/More.tsx`
- `src/components/FeedbackModal.tsx` (신규)
- `supabase/functions/send-feedback/index.ts` (신규)
- `src/App.css` — `.feedback-modal`, `.feedback-editor-wrapper` 등 추가

**배포 완료**
- Edge Function 배포 ✅
- `RESEND_API_KEY` 시크릿 등록 ✅

---

## TODO (이월)

없음 — 260413 TODO 전체 완료
