# 작업 일지 — 2026.05.20

## 기획 검토

- [x] `cardio.app.v2.0.md` 읽고 제도 허점 분석
- [x] `cardio.app.v2.0.loopholes.md` 작성 — 6개 허점 공격 + 반론 수용 후 최종 판정
- [x] `cardio.app.v2.0.scope.md` 작성 — 1차 확정 스코프 + 운영진 단계별 전파 전략

---

## 1차 스코프 확정 내용

### 루키 리그
- 생애 누적 100마일 미달성자 대상
- 100마일 달성 시 졸업 → 클럽 피드 자동 공지 + 영구 뱃지
- (구현 예정)

### 소셜 포인트 — 1차 미적용
- 소셜 포인트 제도 자체는 운영진 전파 후 2~3단계에서 도입 예정

### 월말 시상 구조 확정
- 마일리지 1위 (현행 유지)
- 이달의 커넥터 — 소셜 포인트 1위 (운영진 추천 슬롯 객관화)
- 이달의 다크호스 — 성장률 대시보드 기반 운영진 픽 2~3명
- 졸업자 — 초등(100마일) / 중학교(10km 60분 이내)

---

## 개발 완료

### 다크호스 대시보드
- [x] `supabase/migrations/20260520000001_club_growth_stats_fn.sql`
  - `get_club_growth_stats(club_id, year, month)` RPC 함수
  - 성장률 = (이번달 마일리지 - 전달 일평균 × 경과일수) / 전달 일평균 × 경과일수 × 100
  - 운동일수도 동일 방식으로 전달 정규화 비교
  - 닉네임(`club_nickname`) 포함
- [x] `src/services/clubService.ts` — `getClubGrowthStats()` + `ClubGrowthRow` 타입
- [x] `src/components/ClubGrowthDashboard.tsx`
  - 모바일 카드형 UI
  - 정렬: 성장률 / 운동일수
  - 검색: 클럽 닉네임 기준
  - 카드 표시: 마일리지(전달 N일분 → 이번달) + 운동일수(전달 N일분 → 이번달)
  - 월 이동 (과거 조회 가능)
- [x] `src/pages/Club.tsx` — 클럽 메뉴 → 마일리지 섹션 → 다크호스 대시보드 (vice-manager 이상)
- [x] `src/App.css` — 대시보드 카드 스타일

### 카톡 공유 (운동 기록 후 자동 팝업)
- [x] `src/pages/AddWorkout.tsx` — 저장 완료 후 step 4 추가 (클럽 선택 → 카톡 공유 / 건너뛰기)
- [x] `src/pages/More.tsx` — 더보기 → 앱 설정 → "운동 후 카톡 공유 창 자동 표시" 토글 (기본 ON)
- [x] `src/App.css` — 토글 스위치 + 카톡 공유 step 스타일

---

## 남은 것

- [ ] 루키 리그 UI 구현 (클럽 랭킹 탭 필터/토글)
- [ ] `get_club_growth_stats` SQL — Supabase에 최종 적용 확인
- [ ] 카톡 공유 실기기 테스트
