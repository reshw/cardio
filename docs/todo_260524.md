# 작업 내역 / 패치노트 — 2026-05-24

---

## 패치노트 (유저 공개용)

### ✨ 신규 기능
- **캘린더 운동일수 합계 표시**
  - 월별 캘린더 상단(또는 하단)에 해당 월의 총 운동일수 합계를 함께 표시

- **Vercel 이중 배포**
  - Netlify와 동일 레포로 Vercel 동시 배포 구성

---

## 개발 내부 메모

### TODO / 추후 검토
- [ ] 캘린더 운동일수 합계 표시
  - 표시 위치: 캘린더 상단 또는 하단
  - 표시 형식: 예) "이번 달 운동일수: 12일"
  - 적용 범위: 개인 기록 캘린더 / 클럽 멤버 상세 캘린더 등 확인 필요

- [x] 클럽 랭킹 — 좁은 모바일에서 🏆 배지 줄바꿈 버그 수정

- [x] Vercel 이중 배포 세팅
  - Netlify 유지하면서 동일 GitHub 레포로 Vercel 동시 배포
  - 작업 목록:
    1. `vercel.json` 생성 (빌드 설정 + SPA 라우팅 rewrite)
    2. `api/` 디렉토리 생성 — Netlify 함수 5개를 Vercel 형식으로 포팅
       - send-mileage-alert-email, send-club-request-email, upload-to-r2, create-workout, monthly-snapshot
    3. 프론트엔드 함수 호출 경로 통일: `/.netlify/functions/X` → `/api/X` (2개 파일)
       - `src/utils/email.ts`, `src/utils/r2Storage.ts`
    4. `netlify.toml` 리다이렉트 추가: `/api/*` → `/.netlify/functions/:splat`
    5. devDependencies에 `@vercel/node` 추가
  - 수동 작업 (대시보드): Vercel에서 GitHub 레포 연결 + 환경변수 설정
  - 증상: 마일리지 탭 랭킹 리스트에서 폭이 좁은 화면 시 이름 옆 🏆(hof-badge-inline)가 다음 줄로 밀려 레이아웃 깨짐
  - 수정 포인트 (App.css):
    - `.ranking-left`: `flex: 1; min-width: 0` 추가
    - `.ranking-info`: `min-width: 0; overflow: hidden` 추가
    - `.ranking-name`: `white-space: nowrap; overflow: hidden; text-overflow: ellipsis` 추가
    - `.ranking-right`: `flex-shrink: 0` 추가
  - 주의: master 기준 이슈. dev는 스타일 일부 변경 가능성 있으므로 dev 먼저 확인 후 적용
