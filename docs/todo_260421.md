# Todo - 2026-04-21

## ✅ 이전 완료 항목 (260415)

### 카카오톡 공유 "나도 기록하기" 버튼 상호작용
- [x] 카카오 공유 링크에 `clubId` 및 `feedId` 파라미터 포함
- [x] 링크 진입 시 클럽 소속 여부 체크 로직 구현
- [x] 미가입 시 `/join/{inviteCode}` 리디렉트 처리
- [x] 가입 완료 후 원래 피드 상세 페이지로 복귀 (returnUrl 처리)
- [x] 운동 피드 상세 페이지 정상 노출 확인
- [x] 남의 기록 조회 시 수정/삭제 버튼 미노출 처리

---

## 🐛 버그 수정

### 1. 나도 기록하기 진입 시 — 누구 기록인지 표시
**현상**: 카카오 공유 → 나도 기록하기로 진입했을 때 운동 상세 페이지에 운동 사진은 보이나, 누구의 기록인지 알 수 없음
**수정**: `user.id !== workout.user_id` 일 때 소유자 display_name 조회 → "OOO님의 기록" 배너 표시
- [x] WorkoutDetail 상단에 "OOO님의 기록" 표시

### 2. 나도 기록하기 진입 시 — 뒤로가기 버튼 미작동
**현상**: 카카오 공유 링크로 진입한 WorkoutDetail에서 뒤로가기가 동작 안 함
**원인**: 외부 링크 진입 시 브라우저 히스토리 없음 → `navigate(-1)` 공백 페이지로 이동
**수정**: `window.history.length > 1` 체크 → 히스토리 없으면 `/club`으로 fallback
- [x] WorkoutDetail 뒤로가기 로직 점검 및 수정

### 3. 피드백 전송 실패 (메일 미발송) 버그
**현상**: 더보기 → 수정 요청 / 버그 제보 전송 시 "전송에 실패했습니다" 오류
**원인**: Edge Function이 JWT 검증 필요 상태로 배포됨 → 401 Unauthorized
**수정**: `--no-verify-jwt` 옵션으로 재배포
- [x] Supabase Edge Function 로그 확인 (401 확인)
- [x] 수정 후 재배포 완료

---

## ✨ 기능 개선

### 4. 피드백 접수창 모바일 UX 개선
**현상**: 접수창(FeedbackModal) 모바일에서 직관성 부족 — Jodit 에디터 무거움, 디자인 이상
**수정**: 모바일 바텀시트 방식으로 전면 재설계 (카테고리 칩, textarea, slideUp 애니메이션)
- [x] 입력 폼 단순화 (Jodit → textarea)
- [x] 모달 → 모바일 바텀시트 디자인 개선 (item 9)

---

## 🚀 신규 기능 기획

### 5. 챌린지 기능
**구현**: 개인 챌린지 (기록탭), 클럽 합산 마일리지 표현
- [x] `challengeService.ts` — 조회/진행률계산/생성/삭제
- [x] `ChallengeCard.tsx` — 진행률 바, D-day, 달성 표시
- [x] `PersonalChallengeCard.tsx` — 전달 대비 +10%/+20% 개인 챌린지
- [x] `mileageExpressions.ts` — 클럽 합산 마일리지 재미 표현 (12종, 셔플)
- [x] Club.tsx 마일리지 탭 — 클럽 합산 표현 + 개인 챌린지 카드
- [x] Club.tsx 챌린지 탭 **제거** (History로 이동)
- [x] History.tsx 챌린지 탭 **추가** — 클럽 선택 + PersonalChallengeCard
- [x] 관리자(매니저/부매니저): 챌린지 만들기/삭제 (코드 유지, 탭만 비활성)
- [ ] 그룹 챌린지 (클럽 합산 목표) — 스테이징 서버 적용 예정

### 6. 기록탭 통계 개선
**구현**: 최근 6개월 바 차트 + 월별 비교 카드 + 종목별 차트
- [x] recharts BarChart — 6개월 추이, 막대 클릭으로 월 선택
- [x] 차트 지표 토글 (운동일수/운동횟수/달린거리)
- [x] 월 이동 네비게이션 (‹ ›)
- [x] 비교 카드 3개 — 현재값, 전달값, ▲▼ diff 배지
- [x] 종목별 가로 바 차트

---

## 🗄️ DB 작업

### 7. Prod → 로컬 Docker DB 동기화
- [ ] `pg_dump` prod → `prod_public.dump`
- [ ] `pg_restore` → 로컬 Docker (54322)



### 8. 사진 여러 장 업로드
- 현행 유지 (사진 1장 제한) — 용량 이슈 + 운동 1건 기준 1장으로 충분

### 9. FeedbackModal 디자인 개선
- [x] 모바일 바텀시트 방식으로 전면 재설계 (item 4에 통합 처리)
